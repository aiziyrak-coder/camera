# Grafana host tarmog'ida — Prometheus 127.0.0.1:19090 da.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://127.0.0.1:19090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 30s
      httpMethod: POST
